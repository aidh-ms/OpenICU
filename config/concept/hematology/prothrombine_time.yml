name: prothrombine_time
version: 1.0.0
unit: sec

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: labevents
      event: result
      code: (51274//PT)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
