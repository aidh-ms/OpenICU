name: eosinophils
version: 1.0.0
unit: "%"

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: labevents
      event: result
      code: (51200//Eosinophils)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
