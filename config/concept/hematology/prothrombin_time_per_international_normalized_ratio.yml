name: prothrombin_time_per_international_normalized_ratio
version: 1.0.0
unit: None

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: labevents
      event: result
      code: (51237//INR(PT))
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
