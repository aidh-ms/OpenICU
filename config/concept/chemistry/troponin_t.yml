name: troponin_t
version: 1.0.0
unit: ng/mL

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: labevents
      event: result
      code: (51003//Troponin T)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
