name: vasopressin_rate
version: 1.0.0
unit: ['units/min', 'U/min']

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: inputevents
      event: result
      code: (222315//Vasopressin)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
