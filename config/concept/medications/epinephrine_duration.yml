name: epinephrine_duration
version: 1.0.0
unit: None

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: inputevents
      event: result
      code: (221289//Epinephrine)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
