name: dobutamine_rate
version: 1.0.0
unit: ['mcg/kg/min', 'mcgkgmin']

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: inputevents
      event: result
      code: (221653//Dobutamine)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
