name: sodium
version: 1.0.0
unit: mEq/L

extension_columns:
  dataset: col("dataset")
  table: col("table")

mappings:
  - pattern:
      dataset: mimic-iv
      version: "3.1"
      table: labevents
      event: result
      code: (50983//Sodium)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
