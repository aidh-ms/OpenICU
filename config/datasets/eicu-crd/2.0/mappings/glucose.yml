type: simple
mappings:
  - pattern:
      table: lab
      event: LAB
      code: (glucose(//.*)?)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
  - pattern:
      table: lab
      event: LAB
      code: (bedside\ glucose(//.*)?)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
