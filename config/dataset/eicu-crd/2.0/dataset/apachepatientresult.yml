path: apachePatientResult.csv.gz

columns:
  # subject_id and extensions
  - name: patientunitstayid
    type: int64
  # db fields - values
  - name: apacheversion
    type: string
  - name: acutephysiologyscore
    type: int64
  - name: apachescore
    type: int64
  - name: predictedicumortality
    type: float64
  - name: actualicumortality
    type: string
  - name: predictediculos
    type: float64
  - name: actualiculos
    type: float64
  - name: predictedhospitalmortality
    type: float64
  - name: actualhospitalmortality
    type: string
  - name: predictedhospitallos
    type: float64
  - name: actualhospitallos
    type: float64
  - name: actualventdays
    type: float64
  - name: predventdays
    type: float64

join:
  - name: patient
    path: patient.csv.gz
    columns:
      - name: patienthealthsystemstayid
        type: int64
      - name: patientunitstayid
        type: int64
      - name: hospitaladmitoffset
        type: int64
      - name: hospitaladmittime24
        type: string
      - name: hospitaldischargeyear
        type: int64
    both_on:
      - patientunitstayid
    callbacks:
      - to_datetime(hospitaldischargeyear, 1, 1, hospitaladmittime24, hospitaladmitoffset, "minutes", output=admission_timestamp)

event_defaults:
  subject_id: col(patienthealthsystemstayid)
  time: col(admission_timestamp)
  extension:
    stay_id: col(patientunitstayid)

events:
  - name: APACHE_SCORE
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(apachescore)
  - name: APACHE_APS_SCORE
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(acutephysiologyscore)
  - name: APACHE_PREDICTED_ICU_MORTALITY
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(predictedicumortality)
  - name: APACHE_ACTUAL_ICU_MORTALITY
    columns:
      code:
        - col(apacheversion)
      text_value: col(actualicumortality)
  - name: APACHE_PREDICTED_HOSPITAL_MORTALITY
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(predictedhospitalmortality)
  - name: APACHE_ACTUAL_HOSPITAL_MORTALITY
    columns:
      code:
        - col(apacheversion)
      text_value: col(actualhospitalmortality)
  - name: APACHE_PREDICTED_ICU_LOS
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(predictediculos)
  - name: APACHE_ACTUAL_ICU_LOS
    columns:
      numeric_value: col(actualiculos)
  - name: APACHE_PREDICTED_HOSPITAL_LOS
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(predictedhospitallos)
  - name: APACHE_ACTUAL_HOSPITAL_LOS
    columns:
      numeric_value: col(actualhospitallos)
  - name: APACHE_ACTUAL_VENT_DAYS
    columns:
      numeric_value: col(actualventdays)
    filters:
      - drop_na(numeric_value)
  - name: APACHE_PREDICTED_VENT_DAYS
    columns:
      code:
        - col(apacheversion)
      numeric_value: col(predventdays)
    filters:
      - drop_na(numeric_value)
