path: vitalPeriodic.csv.gz

columns:
  # subject_id and extentions
  - name: patientunitstayid
    type: int64
  - name: vitalperiodicid
    type: int64
  # db fields - times
  - name: observationoffset
    type: int64
  # db fields - values
  - name: cvp
    type: int64
  - name: etco2 # etCo2
    type: int64
  - name: heartrate
    type: float64 # decimal(x,y)
  - name: padiastolic
    type: int64
  - name: pamean
    type: int64
  - name: pasystolic
    type: int64
  - name: respiration
    type: int64
  - name: sao2  # saO2
    type: int64
  - name: st1
    type: float32
  - name: st2
    type: float32
  - name: st3
    type: float32
  - name: icp
    type: int64
  - name: systemicdiastolic
    type: int64
  - name: systemicmean
    type: int64
  - name: systemicsystolic
    type: int64
  - name: temperature
    type: float64 # decimal(x,y)


post_callbacks:
  - add_offset(admission_timestamp, observationoffset, output=event_timestamp)

join:
  - name: patient
    path: patient.csv.gz
    columns:
      # subject_id and extentions
      - name: patienthealthsystemstayid
        type: int64
      - name: patientunitstayid
        type: int64
      # db fields - times
      - name: hospitaladmitoffset
        type: int64
      - name: hospitaladmittime24
        type: string
      - name: hospitaldischargeyear
        type: int64
      # db fields - values
    both_on:
      - patientunitstayid
    callbacks:
      - to_datetime(hospitaldischargeyear, 1, 1, hospitaladmittime24, hospitaladmitoffset, "minutes", output=admission_timestamp)

event_defaults:
  # TODO: this is not actually the subject_id
  subject_id: col(patienthealthsystemstayid)
  time: col(event_timestamp)
  extension:
    stay_id: col(patientunitstayid)
    vitalperiodicid: col(vitalperiodicid)

events:
  - name: HEART_RATE
    columns:
      code:
        - const(BPM)
      numeric_value: col(heartrate)
  # TODO: add unit to all other events here
  - name: TEMPERATURE
    columns:
      numeric_value: col(temperature)
  - name: SAO2
    columns:
      numeric_value: col(sao2)
  - name: RESPIRATORY_RATE
    columns:
      numeric_value: col(respiration)
  - name: CVP
    columns:
      numeric_value: col(cvp)
  - name: ETCO2
    columns:
      numeric_value: col(etco2)
  - name: SYSTOLIC_BP
    columns:
      numeric_value: col(systemicsystolic)
  - name: DIASTOLIC_BP
    columns:
      numeric_value: col(systemicdiastolic)
  - name: MEAN_BP
    columns:
      numeric_value: col(systemicmean)
  - name: PA_SYSTOLIC_BP
    columns:
      numeric_value: col(pasystolic)
  - name: PA_DIASTOLIC_BP
    columns:
      numeric_value: col(padiastolic)
  - name: PA_MEAN_BP
    columns:
      numeric_value: col(pamean)
  - name: ST1
    columns:
      numeric_value: col(st1)
  - name: ST2
    columns:
      numeric_value: col(st2)
  - name: ST3
    columns:
      numeric_value: col(st3)
  - name: ICP
    columns:
      numeric_value: col(icp)
