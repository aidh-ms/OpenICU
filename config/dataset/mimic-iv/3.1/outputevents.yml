name: outputevents
path: icu/outputevents.csv.gz
version: "3.1"
dataset: mimic-iv

columns:
  # subject_id and extentions
  - name: hadm_id
    type: int64
  - name: stay_id
    type: int64
  - name: subject_id
    type: int64
  # db fields - times
  - name: charttime
    type: datetime
    params:
      format: "%Y-%m-%d %H:%M:%S"
  - name: storetime
    type: datetime
    params:
      format: "%Y-%m-%d %H:%M:%S"
  # db fields - values
  - name: caregiver_id
    type: int64
  - name: itemid
    type: int64
  - name: value
    type: float32
  - name: valueuom
    type: string

join:
  - name: d_items
    path: icu/d_items.csv.gz
    columns:
      # db fields - times
      # db fields - values
      # subject_id and extentions
      - name: itemid
        type: int64
      - name: label
        type: string
    both_on:
      - itemid

event_defaults:
  subject_id: col(subject_id)
  extension:
    hadm_id: col(hadm_id)
    stay_id: col(stay_id)

events:
  - name: caregiver_store
    columns:
      time: col(storetime)
      numeric_value: col(caregiver_id)
  - name: dosage
    columns:
      time: col(charttime)
      code:
        - col(label)
        - col(valueuom)
      numeric_value: col(value)
