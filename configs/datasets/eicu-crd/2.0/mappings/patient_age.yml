type: simple
mappings:
  - pattern:
      table: patient
      event: PATIENT_AGE
      code: ^PATIENT_AGE$
    columns:
      numeric_value: col(numeric_value)
    filters:
      - drop_na(numeric_value)
