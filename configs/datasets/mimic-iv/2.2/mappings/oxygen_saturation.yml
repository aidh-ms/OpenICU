type: simple
mappings:
  - pattern:
      table: chartevents
      event: CHART
      code: (220277(//.*)?|226253(//.*)?|220227(//.*)?)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
  - pattern:
      table: labevents
      event: LAB
      code: (50817(//.*)?)
    columns:
      numeric_value: col(numeric_value)
      text_value: col(text_value)
  # TODO: This is another concept but with the same definition-name, therefore we might need a solution if this should not be merged
  # - pattern:
  #     table: chartevents
  #     event: CHART
  #     code: (220277(//.*)?|226253(//.*)?)
  #   columns:
  #     numeric_value: col(numeric_value)
  #     text_value: col(text_value)
