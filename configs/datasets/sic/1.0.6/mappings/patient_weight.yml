# SICdb v1.0.6 -- cases WEIGHT event [ricu weight]; converts to the concept unit (* 0.001)
type: simple
mappings:
  - pattern:
      table: cases
      event: WEIGHT
      code: "(weight)(//.*)?$"
    columns:
      numeric_value: col(numeric_value) * 0.001
